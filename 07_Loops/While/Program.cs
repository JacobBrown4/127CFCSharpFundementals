// Declare a variable
int number = 0;

// Keyword  Condition
while (number <= 100) // If condition is true; loop!
{
    //Loop
    // Print the number and add 5
    Console.WriteLine(number);
    number = number + 5;
}

// If in an infite loop ctrl+c (cmd+c)
